% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{plot_site_composition}
\alias{plot_site_composition}
\title{Land-cover composition chart for one sampling site}
\usage{
plot_site_composition(
  res,
  trap,
  radius = max(res$long$radius_m),
  palette = "viridis",
  file = NULL,
  width = 9,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{res}{Output of \code{\link[=buffer_composition]{buffer_composition()}}.}

\item{trap}{Name of the sampling point.}

\item{radius}{Buffer radius; defaults to the largest computed.}

\item{palette}{\code{"viridis"} (default) orders hue by magnitude. Any other
value is passed to \code{\link[=resolve_palette]{resolve_palette()}} -- \code{"aerial"} makes the bars match
the colours used by \code{\link[=map_composition]{map_composition()}}, \code{"colorblind"} matches the
colour-vision-safe scheme.}

\item{file}{Optional output path.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \pkg{ggplot} object.
}
\description{
Horizontal bars, one per category present in the buffer, ordered by area.
Categories absent from the buffer are omitted, so the panel always fits
whatever the size of the dictionary.
}
\examples{
\donttest{
kml <- system.file("extdata", "example_site.kml", package = "bufferscape")
res <- buffer_composition(kml, radii = 50, grid_res = 5, verbose = FALSE)
if (requireNamespace("ggplot2", quietly = TRUE))
  plot_site_composition(res, "SITE_1")
}
}
