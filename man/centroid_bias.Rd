% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias.R
\name{centroid_bias}
\alias{centroid_bias}
\title{Quantify the centroid approximation bias, polygon by polygon}
\usage{
centroid_bias(
  kml,
  categories = NULL,
  driver = "KML",
  epsg = 31983,
  radii = 50,
  kernel = "exponential",
  lambda = 45,
  grid_res = 1,
  compact_cut = 0.4,
  trap_pattern = "^[A-Za-z]{2,}[_ -]?\\\\d+$",
  verbose = TRUE
)
}
\arguments{
\item{kml}{Path, or vector of paths, to KML files.}

\item{categories}{A category dictionary. \code{NULL} (default) uses the built-in
\link{mare_categories} schema; otherwise a \code{data.frame} or a path to an
\code{.xlsx}/\code{.csv} file. See \code{\link[=class_dictionary]{class_dictionary()}}.}

\item{driver}{GDAL driver. Leave as \code{"KML"} -- see Details.}

\item{epsg}{EPSG code of a \strong{projected} CRS in metres. Default 31983
(SIRGAS 2000 / UTM 23S), appropriate for Rio de Janeiro.}

\item{radii}{Buffer radii in metres. The largest is treated as primary.}

\item{kernel, lambda}{Distance-decay kernel and its scale; see
\code{\link[=decay_kernel]{decay_kernel()}}.}

\item{grid_res}{Resolution in metres of the grid used to integrate the
kernel over polygon geometry. Smaller is more exact and slower; 1 m is
ample for a 50 m buffer.}

\item{compact_cut}{Compactness below which a polygon is called elongated.}

\item{trap_pattern}{Regular expression matching the placemark names of
sampling points.}

\item{verbose}{Print progress.}
}
\value{
A \link[tibble:tibble]{tibble}, one row per polygon per site per radius:
\describe{
\item{\code{Ovitrap_ID}, \code{radius_m}, \code{source_file}}{identifiers}
\item{\code{id_primary}, \code{label_en}}{class}
\item{\code{area_m2}}{clipped area inside the buffer}
\item{\code{d_centroid}}{distance from the site to the polygon centroid (m)}
\item{\code{d_min}, \code{d_max}}{nearest and farthest distance from the site to the
polygon (m)}
\item{\code{centroid_inside}}{\code{FALSE} when the polygon's centroid falls outside
the polygon, as happens for concave features. The centroid weight is then
evaluated at a point that is not part of the feature.}
\item{\code{compactness}}{\eqn{4 \pi A / P^2}; 1 for a circle, towards 0 as the
outline becomes elongated or convoluted}
\item{\code{d_mean}}{area-weighted mean distance from the site to the polygon
(m). This is the quantity the centroid is standing in for.}
\item{\code{centroid_offset}}{\code{(d_mean - d_centroid) / lambda}; how far the
centroid sits from the polygon's mean distance, in kernel scale lengths.
This is the mechanistic driver of the bias: a polygon whose centroid is
much nearer than its typical point gets over-weighted.}
\item{\code{reach_ratio}}{\code{(d_max - d_min) / (d_centroid + 1)}; the span of the
polygon relative to its centroid distance.}
\item{\code{geometry_class}}{\code{"compact"} or \code{"elongated"}, split at
\code{compact_cut}}
\item{\code{w_integrated}, \code{w_centroid}}{mean kernel weight, both ways}
\item{\code{area_w}, \code{area_w_centroid}}{weighted areas}
\item{\code{w_nearest}, \code{area_w_nearest}, \code{bias_nearest_pct}}{the same, for a
weight taken at the polygon's nearest point. Included because it is the
obvious alternative to the centroid; it is an upper bound on the mean
weight and overestimates more than the centroid does, on both compact
and elongated polygons.}
\item{\code{bias_pct}}{\eqn{100 (area\_w\_centroid - area\_w) / area\_w}}
}
}
\description{
Recomputes, for every polygon intersecting every buffer, the distance-decay
weight two ways -- integrated over the polygon's geometry, and evaluated at
its centroid -- and reports the discrepancy alongside shape descriptors.
}
\details{
This is the diagnostic behind the claim that centroid weighting is unsafe.
The bias is not uniform: it is negligible for compact features and large for
elongated ones that pass close to the site, because such a polygon's centroid
can sit almost on the sampling point while most of its area does not.
}
\examples{
kml <- system.file("extdata", "example_site.kml", package = "bufferscape")
b <- centroid_bias(kml, radii = 50, grid_res = 5)
summarise_centroid_bias(b)

}
\seealso{
\code{\link[=summarise_centroid_bias]{summarise_centroid_bias()}}
}
