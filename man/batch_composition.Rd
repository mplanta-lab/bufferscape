% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{batch_composition}
\alias{batch_composition}
\title{Process every KML in a folder}
\usage{
batch_composition(
  kml_dir,
  out_dir = file.path(kml_dir, "Results"),
  pattern = "\\\\.kml$",
  make_maps = TRUE,
  make_charts = TRUE,
  basemap = "none",
  map_zoom = NA,
  radii = c(20, 30, 40, 50),
  kernel = "exponential",
  lambda = 45,
  grid_res = 1,
  epsg = 31983,
  secondary = "weighted",
  sec_weight = 0.3,
  categories = NULL,
  palette = "aerial",
  metrics = c("exact", "weighted", "centroid", "distance"),
  label_ids = 0
)
}
\arguments{
\item{kml_dir}{Folder holding the KML files.}

\item{out_dir}{Output folder; defaults to \verb{Results/} inside \code{kml_dir}.}

\item{pattern}{Regular expression selecting files.}

\item{make_maps, make_charts}{Produce figures.}

\item{basemap, map_zoom}{Passed to the map functions.}

\item{radii, kernel, lambda, grid_res, epsg, secondary, sec_weight}{Passed to
\code{\link[=buffer_composition]{buffer_composition()}}.}

\item{categories}{A category dictionary; see \code{\link[=class_dictionary]{class_dictionary()}}.}

\item{palette}{Passed to \code{\link[=map_composition]{map_composition()}}; \code{"aerial"} (default),
\code{"colorblind"}, \code{"viridis"}, \code{"greyscale"}, or a palette table.}

\item{metrics}{Which surface metrics the workbook should carry; see
\code{\link[=write_composition_report]{write_composition_report()}}.}

\item{label_ids}{Print the class id on this many of the largest polygons in
each map.}
}
\value{
Invisibly, a list with \code{wide}, \code{long}, \code{tanks}, \code{distances}, \code{qc},
\code{summary}, \code{out_dir} and \code{failed}.
}
\description{
Runs \code{\link[=buffer_composition]{buffer_composition()}} over each KML in a directory, writes one workbook
containing every table, one map per sampling site, and the composition and
container charts.
}
\details{
A KML may hold one, two or three sampling points. Buffer polygons are named
per point (\code{VP_9_Buffer}), while land-cover polygons and water containers are
a shared pool for the whole file; each point takes its own elements
\strong{geometrically}, against its own buffer, so per-site outputs never
contaminate one another.

One malformed file does not stop the batch: it is logged and skipped. Sites
whose name appears in more than one file are reported in a
\code{duplicate_traps} sheet, and their figures are given distinct filenames.
}
\examples{
\donttest{
dir.create(d <- tempfile())
file.copy(system.file("extdata", "example_site.kml", package = "bufferscape"), d)
out <- batch_composition(d, radii = 50, grid_res = 5,
                     make_maps = FALSE, make_charts = FALSE)
out$summary
}
}
