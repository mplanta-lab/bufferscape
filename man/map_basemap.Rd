% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{map_basemap}
\alias{map_basemap}
\title{Satellite basemap figure for one sampling site}
\usage{
map_basemap(
  res,
  trap,
  radius = max(res$long$radius_m),
  basemap = "esri",
  zoom = NA,
  file = NULL,
  width = 8,
  height = 8,
  dpi = 300
)
}
\arguments{
\item{res}{Output of \code{\link[=buffer_composition]{buffer_composition()}}.}

\item{trap}{Name of the sampling point to draw.}

\item{radius}{Buffer radius; defaults to the largest computed.}

\item{basemap}{\code{"esri"} for Esri World Imagery, a path to a georeferenced
raster of your own, or \code{"none"}.}

\item{zoom}{Tile zoom level; \code{NA} picks the finest the provider serves.}

\item{file}{Optional output path.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \pkg{ggplot} object.
}
\description{
Imagery with the buffer and the sampling point, and nothing else.
}
\details{
Tiles are requested in the CRS the figure is drawn in, so the raster is never
resampled and cannot drift relative to the polygons. Requires
\pkg{maptiles} and \pkg{terra}; without them the panel renders white and a
warning is issued rather than failing.

Google Earth imagery is not offered: its terms permit alteration only inside
Google software, and it cannot be relicensed for an open-access figure.
}
