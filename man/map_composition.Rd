% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{map_composition}
\alias{map_composition}
\title{Land-cover map for one sampling site}
\usage{
map_composition(
  res,
  trap,
  radius = max(res$long$radius_m),
  palette = "aerial",
  top_n = 10,
  fill_alpha = 0.55,
  patterns = TRUE,
  show_unclassified = TRUE,
  label_ids = 0,
  legend = c("composition", "containers", "key"),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  file = NULL,
  width = 13,
  height = 8,
  dpi = 300
)
}
\arguments{
\item{res}{Output of \code{\link[=buffer_composition]{buffer_composition()}}.}

\item{trap}{Name of the sampling point to draw.}

\item{radius}{Buffer radius; defaults to the largest computed.}

\item{palette}{Colours and textures. A scheme name -- \code{"aerial"} (default),
\code{"colorblind"}, \code{"viridis"} or \code{"greyscale"} -- or a \code{data.frame} with
\code{id}/\code{fill}/\code{pattern}, or a vector of colours named by class id. A partial
palette is allowed: unlisted classes fall back to the dictionary's own
colour. See \code{\link[=class_palette]{class_palette()}}.}

\item{top_n}{Number of classes listed individually in the legend; the rest
are pooled as "other categories".}

\item{fill_alpha}{Fill transparency. Semi-transparent fills let overlapping
polygons show through, which is the point.}

\item{patterns}{Draw the textures defined by the palette. \code{FALSE} gives flat
fills.}

\item{show_unclassified}{Draw and quantify the part of the buffer covered by
no polygon, hatched in grey.}

\item{label_ids}{Print the class id on this many of the largest polygons.
\code{0} (default) prints none. Useful with large dictionaries, where colour
alone cannot separate every class.}

\item{legend}{Which legend sections to show: any of \code{"composition"},
\code{"containers"}, \code{"key"}. \code{FALSE} suppresses the legend entirely and the map
fills the panel.}

\item{title, subtitle, caption}{Text overrides. \code{NULL} uses the automatic text;
\code{""} suppresses that line.}

\item{file}{Optional output path.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \pkg{ggplot} object, invisibly written to \code{file} if given.
}
\description{
A white-ground figure: polygons coloured by category, water containers,
the buffer, and a legend panel giving the surface composition, the container
counts and the distances to reference features.
}
\details{
Polygons are clipped to a square frame set outside the buffer, so features
overshoot the circle as they do in the imagery but never run across the
legend. Overlapping polygons are drawn largest-first, so small features stay
visible. Patterns separate classes that share a base material: dots for
debris, crosshatch for degraded mixed slab, diagonals for active
construction, and grey hatching for unclassified ground.
}
\section{Customising beyond the arguments}{
The return value is an ordinary \pkg{ggplot} object, so anything not exposed
as an argument can be added afterwards -- \code{+ ggplot2::labs()},
\code{+ ggplot2::theme()}, further layers. One exception is worth knowing: the
legend is drawn as text \strong{inside the panel}, not as a ggplot guide, so
\verb{theme(legend.*)} has no effect on it. That is why \code{legend}, \code{top_n} and
\code{title} are explicit arguments.

Drawing deliberately avoids \code{geom_sf()}, \code{coord_sf()} and composition
packages. Geometry is converted to plain coordinates and drawn with
\code{\link[ggplot2:geom_polygon]{ggplot2::geom_polygon()}} under \code{\link[ggplot2:coord_fixed]{ggplot2::coord_fixed()}}, which is the most
version-stable combination and cannot lose layers to CRS renegotiation at
render time.
}

\examples{
\donttest{
kml <- system.file("extdata", "example_site.kml", package = "bufferscape")
res <- buffer_composition(kml, radii = 50, grid_res = 5, verbose = FALSE)
if (requireNamespace("ggplot2", quietly = TRUE)) map_composition(res, "SITE_1")
}
}
