% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{write_composition_report}
\alias{write_composition_report}
\title{Write a composition workbook}
\usage{
write_composition_report(
  x,
  path,
  radii = NULL,
  metrics = c("exact", "weighted", "centroid", "distance"),
  per_radius = TRUE,
  combined = TRUE,
  long = TRUE,
  extras = TRUE,
  key = TRUE,
  labels = c("full_name", "label_en"),
  digits = NULL
)
}
\arguments{
\item{x}{The list returned by \code{\link[=buffer_composition]{buffer_composition()}} or
\code{\link[=batch_composition]{batch_composition()}}.}

\item{path}{Output \code{.xlsx} path.}

\item{radii}{Radii to include. \code{NULL} (default) includes every radius
present. The largest is treated as primary and its sheet is written first.}

\item{metrics}{Which per-class surface metrics to export, any of:
\describe{
\item{\code{"exact"}}{\verb{_area_m2}, the exact area inside the buffer}
\item{\code{"weighted"}}{\verb{_area_w}, kernel integrated over polygon geometry --
the metric intended for modelling}
\item{\code{"centroid"}}{\verb{_area_w_centroid}, kernel at the polygon centroid,
for methods comparison}
\item{\code{"distance"}}{\verb{_d_nearest_m}, distance to the nearest patch of the
class, \code{NA} when absent}
}
Defaults to all four. Dropping \code{"centroid"} roughly halves the width of a
wide sheet if the methods comparison is not needed.}

\item{per_radius}{Write one modelling-ready sheet per radius
(\code{wide_50m}, \code{wide_30m}, ...).}

\item{combined}{Write the single \code{wide} sheet holding every radius at once.}

\item{long}{Write the tidy site x class x radius table.}

\item{extras}{Write the supporting sheets: \code{tanks}, \code{distances}, \code{qc},
\code{categories}, \code{settings}, and any diagnostic sheets present
(\code{unmatched_ids}, \code{failed_files}, \code{duplicate_traps}, \code{buffer_check}).}

\item{key}{Write the \code{metric_key} sheet explaining every column suffix.}

\item{labels}{Class naming in the wide sheets: \code{"full_name"} (default,
\code{id_category_description}, stable and machine-friendly) or \code{"label_en"}
(readable, but changes if the dictionary's labels are edited).}

\item{digits}{Round numeric columns to this many decimals. \code{NULL} leaves
full precision.}
}
\value{
Invisibly, the named list of data frames that was written.
}
\description{
Exports the tables from \code{\link[=buffer_composition]{buffer_composition()}} or \code{\link[=batch_composition]{batch_composition()}} to a
multi-sheet \code{.xlsx} file, with control over which radii, which surface
metrics and which sheets are included.
}
\details{
Exposed as its own function so that a workbook can be produced from a single
\code{buffer_composition()} call, re-exported with different content without
recomputing, or trimmed before sharing.
}
\examples{
\donttest{
kml <- system.file("extdata", "example_site.kml", package = "bufferscape")
res <- buffer_composition(kml, radii = c(30, 50), grid_res = 5,
                          verbose = FALSE)
out <- file.path(tempdir(), "composition.xlsx")
if (requireNamespace("writexl", quietly = TRUE))
  write_composition_report(res, out, metrics = c("exact", "weighted"))
}
}
\seealso{
\code{\link[=buffer_composition]{buffer_composition()}}, \code{\link[=batch_composition]{batch_composition()}}
}
