% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categories.R
\name{class_dictionary}
\alias{class_dictionary}
\title{Land-cover category dictionary}
\usage{
class_dictionary(x = NULL)
}
\arguments{
\item{x}{One of:
\itemize{
\item \code{NULL} (default) -- the built-in \link{mare_categories} schema.
\item a \code{data.frame} -- validated and used as is.
\item a path to an \code{.xlsx} or \code{.csv} file -- read, then validated.
}}
}
\value{
A \code{data.frame} with columns \code{id}, \code{category}, \code{description},
\code{label_en}, \code{fill}, \code{pattern} and \code{full_name}.
}
\description{
A dictionary maps the numeric codes written in the \verb{<description>} field of
each digitised polygon to a named land-cover category. Everything downstream
-- the area columns, the map palette, the legend labels -- is driven by this
table, so the workflow is not tied to any one classification scheme.
}
\details{
The dictionary shipped with the package, \link{mare_categories}, is the 29-class
schema developed for the Complexo da Maré ovitrap study. Supply your own to
work in a different setting.

A dictionary must contain:

\describe{
\item{\code{id}}{integer, unique, positive. The code written in the KML
\verb{<description>} field.}
\item{\code{category}}{character. A coarse grouping (for example \code{"cobertura"},
\code{"vegetacao"}). Used for aggregation and for column names.}
\item{\code{description}}{character. The specific class within the grouping.}
}

and may optionally contain:

\describe{
\item{\code{label_en}}{character. The label printed on maps, charts and
legends. Defaults to \verb{category description}.}
\item{\code{fill}}{character. A hex colour for the map. If absent, a colour is
generated. Supplying your own is strongly preferred for land cover,
because a generated ramp assigns arbitrary hues that carry no meaning.}
\item{\code{pattern}}{character, one of \code{"none"}, \code{"dots"}, \code{"dots2"},
\code{"diag"}, \code{"diag2"}, \code{"cross"}, \code{"horiz"}, \code{"vert"}, \code{"grid"}.
Overprints a texture so that classes sharing a base colour stay
distinguishable. Defaults to \code{"none"}.}
}

A \code{full_name} column (\code{id_category_description}) is added automatically and
is what the output column names are built from.
}
\examples{
# the built-in 29-class schema
head(class_dictionary())

# a minimal custom dictionary
own <- data.frame(
  id          = 1:3,
  category    = c("water", "built", "vegetation"),
  description = c("pond", "roof", "canopy"),
  fill        = c("#2C7FB8", "#BDBDBD", "#31A354")
)
class_dictionary(own)

}
\seealso{
\link{mare_categories} for the built-in schema.
}
