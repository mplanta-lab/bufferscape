% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{plot_group_points}
\alias{plot_group_points}
\title{Water containers per site across a community}
\usage{
plot_group_points(
  tanks,
  community,
  radius = max(tanks$radius_m),
  file = NULL,
  width = 11,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{tanks}{The \code{tanks} table from \code{\link[=buffer_composition]{buffer_composition()}} or
\code{\link[=batch_composition]{batch_composition()}}.}

\item{community}{Value of the site-name prefix to plot, for example \code{"VP"}.}

\item{radius}{Buffer radius; defaults to the largest present.}

\item{file}{Optional output path.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
A \pkg{ggplot} object.
}
\description{
One stacked column per sampling site: sealed tanks, unsealed tanks and
swimming pools, so that the abundance and the nature of the larval habitat
read together at a glance.
}
\details{
Segments are ordered by how available the water is to an ovipositing female
-- sealed, then unsealed, then open pool -- on a viridis ramp. Sealed tanks
usually dominate by an order of magnitude, so a single unsealed tank would
otherwise be a one-pixel sliver: white separators keep every segment legible
and the oviposition-available count is called out above each column.
}
