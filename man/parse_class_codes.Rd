% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categories.R
\name{parse_class_codes}
\alias{parse_class_codes}
\title{Parse a polygon description into primary and secondary category codes}
\usage{
parse_class_codes(x)
}
\arguments{
\item{x}{Character vector of description fields.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{id_primary}, \code{id_secondary} and
\code{n_codes}.
}
\description{
Digitised polygons carry their class in the KML \verb{<description>} field. A
single code (\code{"7"}) is unambiguous; a hyphenated pair (\code{"8-6"}) records a
primary and a secondary class where the interpreter was uncertain or the
surface is genuinely mixed.
}
\details{
Naive coercion silently destroys these: \code{as.numeric("8-6")} is \code{NA}, and an
inner join on \code{NA} drops the polygon without warning. In the Maré data that
would discard roughly a fifth of all polygons, and non-randomly -- the
ambiguous ones are the mixed-material and transitional surfaces.
}
\examples{
parse_class_codes(c("7", " 6 ", "8-6", "5 / 7", "", NA))
}
