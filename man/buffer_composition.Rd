% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape.R
\name{buffer_composition}
\alias{buffer_composition}
\title{Buffer-level landscape metrics for point sampling sites}
\usage{
buffer_composition(
  path = NULL,
  categories = NULL,
  driver = "KML",
  epsg = 31983,
  radii = 50,
  kernel = "exponential",
  lambda = 45,
  grid_res = 1,
  secondary = c("weighted", "primary", "both"),
  secondary_weight = 0.3,
  trap_pattern = "^[A-Za-z]{2,}[_ -]?\\\\d+$",
  tank_pattern = "tank|caixa|reservat",
  tank_open_pattern = "open|abert|descoberta|sem[_ ]?tampa",
  pool_pattern = "pool|piscina|swimming",
  check_kml_buffer = TRUE,
  out_xlsx = NULL,
  verbose = TRUE,
  kml = NULL
)
}
\arguments{
\item{path}{Path, or vector of paths, to \code{.kml} or \code{.gpkg} files. The format
of each file is detected from its contents, so a single call may mix them.}

\item{categories}{A category dictionary. \code{NULL} (default) uses the built-in
\link{mare_categories} schema; otherwise a \code{data.frame} or a path to an
\code{.xlsx}/\code{.csv} file. See \code{\link[=class_dictionary]{class_dictionary()}}.}

\item{driver}{GDAL driver. Leave as \code{"KML"} -- see Details.}

\item{epsg}{EPSG code of a \strong{projected} CRS in metres. Default 31983
(SIRGAS 2000 / UTM 23S), appropriate for Rio de Janeiro.}

\item{radii}{Buffer radii in metres. The largest is treated as primary.}

\item{kernel, lambda}{Distance-decay kernel and its scale; see
\code{\link[=decay_kernel]{decay_kernel()}}.}

\item{grid_res}{Resolution in metres of the grid used to integrate the
kernel over polygon geometry. Smaller is more exact and slower; 1 m is
ample for a 50 m buffer.}

\item{secondary}{How to treat a hyphenated code such as \code{"8-6"}:
\code{"weighted"} gives the primary class \code{1 - secondary_weight} of the area and
the secondary class the rest; \code{"primary"} ignores the secondary code;
\code{"both"} gives the full area to each, so areas double-count deliberately.}

\item{secondary_weight}{Share of the area given to the secondary class under
\code{secondary = "weighted"}.}

\item{trap_pattern, tank_pattern, tank_open_pattern, pool_pattern}{Regular
expressions matching placemark names for sampling points, water tanks,
the unsealed subset of tanks, and swimming pools. Pools are tested first,
so a name like \code{"SwimmingPool"} is never mistaken for a tank. The default
\code{trap_pattern} matches a letter prefix followed by a number
(\code{SITE_1}, \code{VP_9}, \code{NH_12}, \verb{trap 3}); tighten it if your point layer uses
names that could collide with other placemarks.}

\item{check_kml_buffer}{If \code{TRUE}, cross-check any \verb{*_Buffer} polygon in the
file against the computed buffer and report its radius.}

\item{out_xlsx}{Optional path; writes every table to a workbook.}

\item{verbose}{Print progress.}

\item{kml}{Deprecated name for \code{path}, still accepted.}
}
\value{
A list with elements \code{wide} (one row per site, modelling-ready),
\code{long} (site x category x radius), \code{tanks}, \code{distances}, \code{qc},
\code{unmatched_ids}, \code{meta}, \code{categories}, and the projected \code{sf} layers
\code{traps}, \code{polygons}, \code{tanks_sf}, \code{pools_sf}, \code{lines}, \code{buffer_kml}.
}
\description{
Reads one or more KML files, and for every sampling point returns the
land-cover composition of a circular buffer around it, computed directly
from vector polygons.
}
\section{Overlapping polygons}{
Polygons may overlap -- a tree crown over a roof is both -- so category areas
are \strong{not} constrained to sum to the buffer area and proportions are not
computed. A buffer can exceed 100\% classified.
}

\section{Geometry-integrated decay}{
The weighted area \code{area_w} integrates the kernel over each polygon on a
regular grid, then rescales by the exact \code{sf::st_area()}. Evaluating the
kernel at the polygon centroid instead -- reported as \code{area_w_centroid} for
comparison -- is badly biased for elongated features passing close to the
sampling point, whose centroid sits at \eqn{d \approx 0} while most of their
area does not. On Maré data the bias reaches 45\% for an internal road, while
compact roofs stay under 1\%.
}

\section{Why the KML driver}{
GDAL defaults to LIBKML, which overrides \verb{<name>} with an ExtendedData field
of the same name. A buffer polygon named \code{VP_21_Buffer} then reads as
\code{VP_21}, is no longer recognised as reference geometry, and silently enters
the land-cover totals as an unclassified polygon. Forcing \code{driver = "KML"}
reads \verb{<name>} correctly.
}

\section{Nothing is dropped silently}{
Unparseable descriptions, category codes absent from the dictionary, and
points matching no pattern all raise warnings and are reported in the \code{qc}
and \code{unmatched_ids} tables. Distances that were not measured stay \code{NA} and
are never coerced to zero.
}

\examples{
kml <- system.file("extdata", "example_site.kml", package = "bufferscape")
res <- buffer_composition(kml, radii = 50, grid_res = 5, verbose = FALSE)
res$tanks
head(res$long[res$long$area_m2 > 0, c("full_name", "area_m2", "area_w")])

}
\seealso{
\code{\link[=batch_composition]{batch_composition()}} to process a folder, \code{\link[=class_dictionary]{class_dictionary()}} for
custom dictionaries.
}
